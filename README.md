# iGraphite — документація проєкту

## Короткий опис

Корпоративний сайт виробника графітової продукції — **igraphite.pl** (Польща).
Двомовний: польська версія за замовчуванням (корінь репозиторію), англійська — у `/en`.
Наразі це **повністю статичний HTML/CSS/JS сайт** (верстка на базі готового шаблону), без CMS
і без бази даних для контенту. Єдина динамічна частина — форма контактів на PHP.

Продакшн: shared-хостинг, домен http://www.igraphite.pl/

## Архітектура

```
/                       — польська версія (за замовчуванням)
  index.html, o-nas.html, kontakt.html, kontakt2.html,
  grafit*.html, zastosawania-*.html (застосування графіту),
  blog*.html, jakosc-i-bezpieczenstwo.html, gatunky_grafitu_pl.html, ...
/en                     — англійська версія, дзеркалить структуру PL-сторінок
  index.html, about-us.html, contacts.html, products*.html,
  applications-*.html, graphite*.html, ...
  посилається на спільні ресурси через ../assets/
/assets                 — спільні статичні ресурси для обох мовних версій
  css/, scss/           — стилі (vendor + власний style.css)
  js/                   — vendor + functions.js (тут AJAX-логіка форми контактів)
  images/, fonts/       — медіа
  docs/                 — прайс-лист (docx/pdf)
  php/                  — contact.php (обробник форми, без SMTP/OAuth) + бандл PHPMailer
  phpmailer_src/        — окрема повна копія репозиторію PHPMailer (vendor/, composer.json) —
                          звідси реально працює "жива" форма (Gmail OAuth2)
/en/assets/php          — копія PHP-обробників для англомовної версії
robots.txt, sitemap.xml — SEO
```

### Форма контактів (важливо)

На сайті одночасно існує кілька обробників форми — це не помилка дублювання,
а різні шляхи відправки:

- **Основний шлях (JS увімкнено):** `assets/js/functions.js` → AJAX POST на
  `https://igraphite.pl/assets/phpmailer_src/index.php` → PHPMailer через Gmail SMTP
  з OAuth2 (XOAUTH2). Refresh-токен зберігається в MySQL
  (`assets/phpmailer_src/class-db.php`).
- **Фолбек (JS вимкнено):** форма `kontakt.html` без AJAX відправляється напряму на
  `assets/phpmailer_src/contact.php` (простий `mail()`, без SMTP/OAuth).
- Є ще схожий `assets/php/contact.php` (через `PHPMailerAutoload`, без SMTP/OAuth) —
  призначення цього дубліката остаточно не з'ясоване, варто перевірити, чи він
  реально використовується.

### Секрети (навмисно НЕ в git)

Ці файли містять **живі** облікові дані (підтверджено власником проєкту) і виключені
через `.gitignore`. На продакшн-сервері вони лишаються без змін, сайт працює як і раніше:

- `assets/php/PHPMailer/get_oauth_token.php`
- `assets/phpmailer_src/get_oauth_token.php`
- `assets/phpmailer_src/index.php`
- `assets/phpmailer_src/class-db.php` — Google OAuth Client ID/Secret + пароль MySQL
  до БД `igraphite`, де зберігається живий refresh-токен Gmail-акаунту
  `tolik.kostyuchok@gmail.com`

Статус ротації:
1. **Google OAuth Client ID/Secret** — ✅ зроблено 2026-07-21: старий Client ID видалено
   в Google Cloud Console, створено новий, `index.php`/`get_oauth_token.php` (обидві
   копії) оновлені й на сервері, і локально.
2. **MySQL-пароль** (`u_igraphite` / `class-db.php`) — ⏸ призупинено: в панелі хостингу
   не знайдено користувача з іменем `u_igraphite` (можливо, інший префікс, або форма
   вже давно не працює). Ротацію та підключення нового refresh-токена (через
   `get_oauth_token.php` у браузері) свідомо відклали — форма контактів наразі не в
   пріоритеті, до неї повернемось окремо.

## Продакшн / хостинг

- Домен: http://www.igraphite.pl/, shared-хостинг на базі CloudLinux (CageFS,
  PHP-селектор), SSH-доступ є (алiас `igraphite` в `~/.ssh/config`, ключ
  `~/.ssh/igraphite_wpotas`)
- **Реальна PHP-версія сайту: 7.4.33** (перевірено прямим HTTP-запитом; CLI за
  замовчуванням — застарілий 5.6.40, на нього орієнтуватись не треба). На сервері
  доступні версії 5.2–8.5 через per-domain селектор — при переїзді на WordPress
  варто підняти домен на 8.x.
- Composer і MySQL client доступні по SSH
- **WP-CLI встановлено на сервері й перевірено (`wp --version` → 2.12.0,
  2026-07-23).** Це означає, що контент сторінок/постів (`post_content` у
  БД — форми, картки, таблиці на wp-new) можна редагувати напряму командами
  (`wp post update`, `wp post get --field=content` тощо) через SSH-alias
  `igraphite`, **без потреби копіювати HTML вручну через wp-admin**.
  Раніше (сесія 2026-07-23) вважалося, що є лише FTP/SFTP-доступ без shell —
  це виявилося помилковим припущенням; SSH завжди був повноцінним shell'ом,
  просто не перевірявся напряму. Не повторювати цю помилку — перевіряти
  `ssh igraphite` замість здогадок.
- Веб-корінь: `~/igraphite.pl/www/` на сервері
- ⚠️ Знайдено й усунуто: `www.zip` (25 МБ бекап із живими секретами всередині) лежав
  прямо в публічному веб-корені й віддавався по HTTP — перенесено в
  `~/private_backups/`, більше не публічний (404). Ще один аргумент за ротацію
  креденшелів вище.

## Git / GitHub

- Репозиторій: https://github.com/Kostiuchok/igraphite (публічний)
- Гілка `main` — прямий імпорт поточного продакшн-коду (перший комміт)
- Push Protection активний на GitHub — блокує пуш із виявленими секретами

## Як працювати з проєктом

- Сторінки — статичний HTML, редагуються напряму.
- `/assets` спільний для PL і EN: зміни в css/js/images стосуються обох мовних версій
  одночасно. Текстовий контент і розмітку сторінок редагують окремо в корені (PL)
  та в `/en` (EN) — синхронізувати вручну.
- Локальний перегляд: сайт статичний, HTML можна відкривати прямо в браузері;
  для тестування PHP-форми — підняти простий сервер, напр. `php -S localhost:8000`.
- Перед комітом обов'язково перевіряти `git status`/вміст нових файлів на реальні
  секрети (ключі, паролі, токени) — Push Protection ловить не все.

## Завдання (поточний стан)

1. ✅ Підключити проєкт до GitHub
2. 🔶 Ротувати виявлені живі креденшели — Google OAuth зроблено, MySQL-пароль відкладено
3. ✅ З'ясувати технічні обмеження shared-хостингу — PHP 7.4.33 live (селектор 5.2–8.5),
   Composer + MySQL client є, SSH-доступ налаштовано (алiас `igraphite`)
4. ✅ Обрано підхід до адмін-панелі: **WordPress** — головний критерій власника
   проєкту: зручність для нетехнічного редактора контенту (не швидкість міграції).
   Причини: готова адмінка, Polylang для PL/EN, Yoast SEO, великий досвід у
   спільноти під shared-хостинг. Компроміс: потрібна повна міграція ~30 сторінок
   у WP-шаблони/пости.
5. 🔶 Міграція на WordPress (стейджинг `igraphite.pl/wp-new/`, PHP 8.5):
   - ✅ WordPress встановлено, тема `igraphite` (wordpress/themes/igraphite) підключає
     наявні `/assets/css`, `/assets/js`, зображення без дублювання
   - ✅ Polylang + Yoast SEO встановлено й активовано (мовне налаштування Polylang —
     власник проєкту робить сам)
   - ✅ Перенесено 24 PL-сторінки/пости (21 сторінка + 3 пости блогу) з розбором
     оригінального HTML, виправленням шляхів assets і внутрішніх посилань
   - ✅ Головне меню зібрано (Spółka/Grafit/Zastosowania dropdown + Blog/Kontakt),
     чисті permalinks (`/%postname%/`), головна сторінка встановлена
   - ✅ EN-версія (11 сторінок з наявних, оригінальний EN-сайт неповний — багато
     пунктів меню на живому сайті вели на неіснуючі сторінки) — перенесено й
     зв'язано як переклади відповідних PL-сторінок через Polylang
     (`pll_save_post_translations`), англомовне меню зібране
   - ✅ Знайдено й виправлено 2 бага верстки під час QA (Playwright): застарілий
     `page.php` на сервері (забув перезалити після спрощення) та відсутній
     `single.php` для постів блогу (показував лише заголовок без тексту)
   - 🔶 Відомий нюанс: `/wp-new/en/` 301-редіректить на `/wp-new/en/home-en/`
     замість прямої віддачі — Polylang не показує переклад фронтпейджа на
     кореневому URL мови без додаткового налаштування "статична головна
     сторінка" per-language; не блокер, але варто поправити перед продакшеном
   - ⬜ Вичитка контенту власником, виправлення верстки там, де оригінальні секції
     не покриті дженерик-шаблоном
6. ✅ Новий обробник форми контактів: замінено весь старий ланцюжок
   (`assets/phpmailer_src/*` з живим Google OAuth + MySQL) на власний легкий
   handler у темі (`functions.php`, `admin_post_igraphite_contact`) через
   `wp_mail()` — без OAuth, без збережених секретів. Форму на сторінках
   `kontakt`/`contacts` перепідключено (клас переназвано, щоб старий спільний
   `/assets/js/functions.js` її більше не перехоплював; додано honeypot-поле
   проти спаму). Перевірено наскрізним тестом — лист іде на
   `tolik.kostyuchok@gmail.com`.
   - Старі файли з живими креденшелами (`get_oauth_token.php`, `index.php`,
     `class-db.php`) більше ніде не використовуються в новій версії сайту —
     після переходу на прод їх можна безпечно видалити з сервера
7. ⬜ Налаштувати процес деплою з GitHub на shared-хостинг (FTP/SFTP чи git-хуки —
   залежно від можливостей хостингу)

## Чекліст на наступну сесію

- [ ] Вичитка контенту власником (PL + EN сторінки на стейджингу
      `igraphite.pl/wp-new/`) — позначити, де верстка/текст не так
- [x] Polylang: `/en/` 301-редіректить на `/en/home-en/` замість прямої
      віддачі — з'ясовано остаточно: це **закладена поведінка Polylang**
      (`PLL_Links_Permalinks::front_page_url()`), не баг. При `hide_default
      = true` чистий кореневий URL отримує лише дефолтна мова (pl); для
      інших мов "головна" — це звичайна сторінка зі своїм slug + мовним
      префіксом. Щоб прибрати цей редірект, довелось би вимкнути
      `hide_default` (тоді PL теж отримає префікс `/pl/`) або писати
      кастомний filter override — визнано недоцільним, залишаємо як є
      (для відвідувача працює коректно).
      Заразом знайдено й виправлено 2 реальні проблеми:
      1. Стейджинг (`siteurl`/`home`) працював на `http://` замість
         `https://` — виправлено (WP-опції + увімкнено redirect http→https
         в панелі хостингу; SSL-сертифікат сам по собі був справний).
      2. Через це в кеші Polylang (`pll_languages_list` transient)
         застрягли старі `http://`-адреси в `home_url` для кожної мови —
         тому після виправлення siteurl сторінки якийсь час віддавались
         **без верстки** (браузer блокував http CSS/JS на https-сторінці
         як mixed content). Видалили застарілий transient, Polylang
         перегенерував дані на `https://` — перевірено на PL і EN
         сторінках, http-посилань не лишилось.
- [x] Файли з живими креденшелами видалено з продакшн-сервера (2026-07-22):
      `assets/phpmailer_src/{index.php,get_oauth_token.php,class-db.php}`,
      `assets/php/PHPMailer/get_oauth_token.php` — перевірено, всі повертають
      404. ⚠️ Побічний ефект: основний (AJAX) шлях форми контактів на
      **живому статичному сайті** (не стейджингу) зараз не працює, бо він
      слав запит саме на видалений `index.php` — свідомо залишили як є до
      переходу на прод WordPress (фолбек `contact.php` без AJAX досі
      робочий). При переїзді на прод цей момент відпадає сам собою.
      Файл `en/assets/php/PHPMailer/get_oauth_token.php`, що є в git,
      перевірено — містить лише плейсхолдери (`RANDOMCHARS-----`), реального
      секрету в репозиторії немає.
- [x] MySQL-пароль (`u_igraphite`) — власник проєкту перевірив у панелі
      хостингу: користувача з таким іменем (і варіаціями з префіксом) не
      існує. Ротувати нічого — питання закрито, `class-db.php` з паролем
      уже видалено з проду
- [x] Биті внутрішні посилання по всьому мігрованому контенту (2026-07-22):
      увесь контент був перенесений "як є" зі старого сайту з абсолютними
      URL без урахування підпапки `/wp-new/` (напр. `/o-nas/`,
      `/en/blog/`, старі `.html`-файли типу `grafit-właściwości.html`).
      213 замін по 31 сторінці/посту — усі внутрішні посилання (хлібні
      крихти, тіло контенту, головна сторінка) тепер коректно ведуть на
      `/wp-new/...`.
      Додатково знайдено ~19 посилань, що вели на сторінки, яких ніколи
      не існувало (успадковано з оригінального неповного EN-сайту:
      `products-automotive`, `products-refractories`, `expandable-graphite`
      тощо). Власник проєкту дав відповідність цих назв до 10 реальних
      PL-сторінок каталогу `zastosawania`. Створено 8 нових EN-сторінок
      (переклад PL-контенту, зв'язані через Polylang як переклади):
      `products-refractories`, `products-carbon-brushes`,
      `products-heat-insulation`, `products-automotive`, `products-friction`,
      `products-formed-graphite-parts`, `products-lubricants`,
      `projects-standard` (Traditional use). Мертві посилання на сторінках
      `products`/`products-batteries` перенаправлено на них.
      Залишились без EN-перекладу (жодне посилання на них не веде):
      "Grafit żyłowy", "Tradycyjne zastosowanie" — вже покрито окремо
      вище.
- [x] Головне меню (2026-07-22): виявлено 2 нові проблеми під час QA:
      1. Меню взагалі не рендерилось на жодній сторінці (PL і EN) —
         причина: `polylang.nav_menus['igraphite']['primary']` було
         порожнім масивом, тому Polylang скидав прив'язку меню в 0 на
         кожному реальному запиті (хоча в звичайному селекторі локації
         меню показувалось прив'язаним). Одне мігроване меню "Głowne
         menu" (22 PL-пункти) насправді містило впереміш і 8 EN-пунктів —
         розділили на два окремих меню ("Głowne menu" — PL, нове "Main
         Menu EN" — EN) і прописали обидва в `polylang.nav_menus` напряму
         через WP-CLI.
      2. Дропдауни (Spółka/Grafit/Zastosowania, Graphite/Products)
         відображались завжди розкритими на всю висоту замість
         normal hover-дропдауну — CSS сайту стилізує `.has-dropdown`/
         `.dropdown-menu` (Bootstrap-стиль з оригінального шаблону), а
         WordPress `wp_nav_menu()` за замовчуванням генерує
         `.menu-item-has-children`/`.sub-menu`. Додано 3 filter-хуки в
         `functions.php` (`nav_menu_css_class`, `nav_menu_submenu_css_class`,
         `nav_menu_link_attributes`), щоб WP генерував очікувані класи
         та `data-toggle="dropdown"` для мобільної версії.
      ⚠️ Відомий нюанс: EN-меню зараз має власні коректні EN-пункти
      (About us/Graphite/Products/Blog/Contacts), але футер (`footer.php`)
      і досі показує тільки PL-посилання на EN-сторінках (окрема, менш
      критична проблема — не займались).
- [ ] Перехід на прод:
  - [ ] Перенести WordPress з `/wp-new/` у корінь домену
  - [ ] Оновити захардкожені шляхи, залежні від `/wp-new/` (форма контактів
        зараз шле на `/wp-new/wp-admin/admin-post.php`)
  - [ ] Заархівувати (не видаляти одразу) старі статичні файли
  - [ ] Видалити з сервера файли з мертвими креденшелами
        (`get_oauth_token.php`, `index.php`, `class-db.php`)
- [ ] Визначити процес деплою з GitHub на хостинг надалі (FTP/SFTP при
      пуші, чи вручну)
